# Ouro Pass 升级脚本 deploy/update.sh（简单可靠，容忍短暂停机）

Spec-ID: S0012
Status: completed
Created Time: 2026-06-29T10:00:00+08:00
Start Time: 2026-06-29T10:00:00+08:00
Completion Time: 2026-06-29T16:30:00+08:00
Previous Spec-ID: S0011
Closure Reason: delivered

> 为已部署用户提供一条命令的**升级**:热备 → (可选)指定版本 → 拉镜像 → 重建 → 健康校验 → 失败给回滚指引。经用户确认:这是**去中心化自托管**场景,**简单可靠优先**,可容忍 `docker compose up -d` 的几秒重启空窗;**不做**零停机(Caddy 重试 / 蓝绿)——因为单体内嵌 worker 无多实例协调(无 SKIP LOCKED/leader;Telegram getUpdates 409;push 双发),两实例并行不安全,且复杂度不值。零应用代码改动。

## 1. Requirement Details

### Background
- 当前升级靠手动 `docker compose pull && up -d`;缺少升级前备份与失败回滚指引。
- 架构约束(已核实):push/reconciliation/telegram worker 无 DB 锁/leader 协调,两实例并行会重复副作用 → 真零停机需改应用(workers-off + 向后兼容迁移),不在本期。
- issuer 已优雅停机(SIGTERM→`srv.Shutdown(ShutdownTimeout)`、worker join)、启动自动迁移、有 `/healthz` HEALTHCHECK;镜像无 v 前缀 tag(`0.x.0`/`latest`)。

### Scope
- 新增 `deploy/update.sh`(POSIX sh,沿用 init.sh 的 ROOT 解析:在安装目录运行)：
  1. **前置**:docker、`docker compose`;`.env`、`docker-compose.yml` 存在;从 `.env` 读 `POSTGRES_USER/DB`、当前 `OUROPASS_TAG`。
  2. **热备**(默认开,`--no-backup` 关):postgres 在跑则 `docker compose exec -T postgres pg_dump` 逻辑备份 → `backups/db-<ts>.sql.gz`,并 `cp .env backups/env-<ts>.bak`(`.env` 含 FIELD_KEY,务必留存);不在跑则告警跳过。
  3. **指定版本**(`--tag X`):改 `.env` 的 `OUROPASS_TAG=X` 再升级;不传则用 `.env` 现值(如 `latest`)。
  4. **拉取 + 重建**:`docker compose pull`(旧容器照常服务)→ `docker compose up -d`(接受几秒重启;旧容器优雅 drain,新容器启动并自动迁移)。
  5. **健康等待**:轮询 issuer 容器 `HEALTHCHECK` 直到 healthy(超时阈值)。
  6. **失败回滚指引**:不健康/超时 → 打印回滚步骤(把 `OUROPASS_TAG` 改回旧值 + `up -d`;必要时从 `backups/db-<ts>.sql.gz` 恢复)并非零退出。
- `set -eu` + 错误 trap;幂等(可重复运行)。
- README/docs 升级章节接入 `update.sh`。

### Constraints
- C1 **零应用代码改动**:仅新增 `deploy/update.sh` + 改 `README.md`/`docs/deployment.md`。
- C2 **简单可靠**:不引入 Caddy 重试 / 蓝绿 / 双实例;容忍短暂停机。
- C3 **数据安全**:升级前热备(一致性逻辑快照 + .env);回滚指引明确;迁移前向(forward-only),回滚镜像后若 schema 已变需从备份恢复(文档警示)。
- C4 **POSIX sh 零硬依赖**:docker/gzip(busybox 即可)之外不强求。

### Non-goals
- 零停机 / 蓝绿 / 滚动(需改应用,见 S0011 讨论;未来如需另立 spec)。
- 自动定时升级、自动回滚执行(仅给指引,不自动 restore)。
- 备份上传远端 / 加密备份(本地 `backups/` 即可,文档提示异地留存)。

## 2. Outline Design
```sh
#!/usr/bin/env sh
set -eu ; ROOT=$(cd "$(dirname "$0")/.." && pwd) ; cd "$ROOT"
flags: --tag X | --no-backup | -h
preflight: docker, docker compose, .env, docker-compose.yml
read .env: PGUSER/PGDB, OLD_TAG=current OUROPASS_TAG
[backup] postgres running? -> mkdir backups; pg_dump | gzip > backups/db-<ts>.sql.gz ; cp .env backups/env-<ts>.bak
[--tag]  set_env OUROPASS_TAG X
docker compose pull
docker compose up -d                 # brief restart accepted
wait_health issuer (poll docker inspect .State.Health.Status, timeout ~90s)
  healthy -> success + version
  else    -> rollback hints (revert OUROPASS_TAG=$OLD_TAG + up -d ; restore db from backup) + exit 1
```
- `set_env`(awk 就地替换/追加)、`on_exit` trap(沿用 init/install 风格)。
- 时间戳用宿主 `date +%Y%m%dT%H%M%S`。

### 2.3 Risk and rollback
- R1 pg 未运行导致备份失败中断升级 → 检测容器状态,未跑则告警跳过(不中断)。
- R2 新版本不健康 → 健康等待超时 → 明确回滚指引(改 tag + up;DB 从备份恢复);不自动改动。
- R3 迁移前向不可逆 → 文档警示:回滚镜像后如 schema 已变,需 restore 备份。
- Rollback:纯新增脚本 + 文档,`git revert` 即可。

## References
- deploy/init.sh、deploy/install.sh — ROOT 解析 / set_env / on_exit 风格复用
- docker-compose.yml、.env.example — 服务名/变量
- cmd/issuer/main.go — 优雅停机 + 启动迁移 + /healthz
- docs/specs/completed/20260628T1400-S0011-one-line-installer.md — 前序

## 3. Execution Plan
### p1 升级脚本
- [x] p1-1 `deploy/update.sh`（前置 + 热备 pg_dump + `--tag` + pull + up + 健康等待 + 回滚指引；POSIX、set -eu+trap、幂等）+ `install.sh` 一并下载 `update.sh`（TC-1, TC-2, TC-3, TC-4）。

### p2 文档接入
- [x] p2-1 `README.md` + `docs/deployment.md` 升级章节改用 `update.sh`（含热备/--tag/回滚说明 + 迁移前向警示）（TC-5）。

### p4 评审后修复（multi-agent-review P2 remediation）
- [x] p4-1 修复 multi-agent-review（claude+codex+cursor）的 6 个 P2：update.sh `--tag` 改 .env 前先 `docker pull` 验证（失败不动 .env）；备份 fail-closed（Postgres 未运行即中止，除非 `--no-backup`）；`.env.example` 加 DSN URL-safe 提示；install.sh 在 ACME_EMAIL 非空时向 Caddyfile 预置 `email` 块（空值会让 Caddy 报错）；install.sh 重跑检测既有 `.env` 不覆盖运维配置；README/deployment 手动清单补 `deploy/update.sh`（顺带 install.sh 去掉 tag 前导 v）（TC-1, TC-2, TC-4, TC-5）。

### p3 验收
- [x] p3-1 验收：`shellcheck` + `sh -n`；起最小栈(postgres+issuer)实跑 update.sh——备份文件生成、`--tag` 改 .env、pull+up、健康等待至 healthy；不健康分支打印回滚指引核对（TC-1..TC-5 汇总）。

## 4. Test and Acceptance Criteria
- TC-1 **脚本质量**：`sh -n` + `shellcheck --shell=sh deploy/update.sh` 无 error。
- TC-2 **热备**：postgres 运行时 `update.sh` 生成 `backups/db-<ts>.sql.gz`(非空、可 gunzip)+ `env-<ts>.bak`;postgres 未运行则告警跳过、不中断。
- TC-3 **升级生效**：`--tag X` 改 `.env` 的 `OUROPASS_TAG=X`;`docker compose pull` + `up -d` 后 issuer 容器 HEALTHCHECK 达 healthy。
- TC-4 **失败/回滚**：新容器超时不健康时,打印回滚指引(改回旧 tag + up;从备份恢复)并非零退出;`set -eu`+trap;重复运行不破坏。
- TC-5 **文档**：README/deployment 升级章节用 `update.sh`,含热备/--tag/回滚/迁移前向警示,命令与脚本变量一致。
- Pass/fail：每 item 仅在其映射 TC 全 pass 且证据 append 后标 `[x]`；p3-1 总收口。

## 5. Execution Log (append-only)
- 2026-06-29 p4-1 完成（评审后修复）：依 `code_review/S0012-deploy-stack/summary.md` 修 6 个 P2——(1) `update.sh --tag` 在 `set_env` 前 `docker pull "$IMAGE:$NEWTAG"` 验证,失败 `err` 且不改 `.env`;(2) 备份 fail-closed:Postgres 未运行由 warn-skip 改为 `err`(除非 `--no-backup`);(3) `.env.example` POSTGRES_PASSWORD 加"仅 URL 安全字符"提示;(4) `install.sh` 在 `ACME_EMAIL` 非空且 Caddyfile 无 email 块时 `printf` 预置 `{ email {$ACME_EMAIL} }`(空值会让 Caddy adapt 报错——已实测);Caddyfile 注释相应更新、去掉误导的注释块;(5) `install.sh` 在 init 前记录 `ENV_PREEXISTED`,重跑时跳过配置写入、仅补密钥并从 .env 读回 DOMAIN,避免覆盖运维配置;(6) README + deployment.md 手动文件清单补 `deploy/update.sh` + chmod;另把 install.sh 的 TAG 输入去前导 `v`。shellcheck CLEAN(SC2016 单引号占位符已 disable)。实测:T1 坏 tag→.env 不变(exit1)、T2 pg 停→fail-closed(exit1)、T3 重跑 DOMAIN 保留 + 守卫提示、T4 email 预置后 `caddy validate`=Valid。`cursor` 的两个 P1(eval 注入/trap 退出码)经实测为误报,不修。
- 2026-06-29 p3-1 完成（总收口）：`sh -n` + `shellcheck --shell=sh` 对 update.sh 与(改动后) install.sh 均 CLEAN；最小栈 e2e 实跑通过(备份/--tag/pull+up 仅重建 issuer/健康等待 healthy)；回滚分支代码核对;文档接入一致。零应用代码改动。注:`install.sh` 现 fetch `update.sh`,需 update.sh 进 main 后该 fetch 才不 404(push 后即可;钉 tag 链路待下次发版)。
- 2026-06-29 p2-1 完成：`docs/deployment.md` Operations 加「### Updating」小节(用 `./deploy/update.sh`，含 --tag/--no-backup、`./backups/` 说明、**迁移前向 + 备份恢复**回滚警示、手动等价步骤);`README.md` Quick start 后加「### Updating」简述(update.sh + --tag + 回滚提示 + 链接 deployment#updating)。命令与脚本一致。
- 2026-06-29 p1-1 完成：`deploy/update.sh`（POSIX sh，`set -eu`+`on_exit` trap）——`--tag`/`--no-backup`/`-h`；preflight(docker/compose/.env/compose.yml)；从 .env 读 PGUSER/PGDB/OLD_TAG；默认热备(postgres 运行则 `pg_dump | gzip → backups/db-<ts>.sql.gz` + `cp .env backups/env-<ts>.bak`，失败则中止；不在跑则告警跳过)；`--tag` 经 `set_env` 改 OUROPASS_TAG；`docker compose pull` + `up -d`(仅 issuer 重建)；轮询 issuer HEALTHCHECK(~90s)；不健康打印日志 + 回滚指引(改回 OLD_TAG + up；从备份 restore)并退 1。`install.sh` 增 `fetch deploy/update.sh`+chmod，使安装目录自带 updater。验证：sh -n + shellcheck CLEAN；最小栈(postgres+issuer)实跑 `update.sh --tag latest`——备份 db-….sql.gz(3243B,gzip -t 通过)+env bak、OUROPASS_TAG=latest、pull、仅 issuer 重建、健康等待至 healthy、"Update complete"。
- 2026-06-29 S0012 创建并激活（active）：前序 S0011 已 delivered。范围经用户确认 = 简单可靠的升级脚本(容忍短暂停机)，含热备 pg_dump / --tag 指定版本 / 健康等待 + 回滚指引；明确不做零停机(架构无 worker 多实例协调)。

## 6. Validation Evidence (append-only)
- （待执行后按 `TC-<n> | stack: shell|docker|other | command: ... | result: pass|fail | note: ...` 追加）

- TC-1 | stack: shell | command: sh -n + shellcheck --shell=sh deploy/update.sh | result: pass | note: 语法 OK；shellcheck CLEAN(SC2329 trap 误报已 disable)
- TC-2 | stack: docker | command: 最小栈实跑 update.sh(postgres 运行) | result: pass | note: 生成 backups/db-<ts>.sql.gz(3243B,gzip -t 通过)+ env-<ts>.bak
- TC-3 | stack: docker | command: update.sh --tag latest | result: pass | note: .env OUROPASS_TAG=latest；docker compose pull + up 仅重建 issuer；HEALTHCHECK 达 healthy；"Update complete"
- TC-4 | stack: shell | command: review 回滚分支 + set -eu/trap | result: pass | note: 不健康打印近 30 行日志 + 回滚指引(改回 $OLD_TAG + up；gunzip backup | psql restore)并退 1；备份失败中止保护数据
- TC-5 | stack: other | command: review README.md + docs/deployment.md | result: pass | note: 升级章节用 update.sh，含热备/--tag/回滚 + 迁移前向警示；命令与脚本变量一致
- TC-1..TC-5 | stack: shell/docker | command: p3-1 汇总 | result: pass | note: shellcheck CLEAN(install+update)、最小栈 e2e 通过、回滚分支核对、文档一致、交付齐备

- TC-1 | stack: shell | command: sh -n + shellcheck --shell=sh install.sh update.sh | result: pass | note: p4-1 后 CLEAN(SC2016 占位符 disable)
- TC-2 | stack: docker | command: update.sh --tag 0.0.0-bad --no-backup | result: pass | note: docker pull 验证失败 → err 退1，.env OUROPASS_TAG 未改(latest→latest)
- TC-2 | stack: docker | command: update.sh（默认，postgres 未运行） | result: pass | note: fail-closed，err "postgres is not running" 退1，不继续 pull/up
- TC-4 | stack: docker | command: 预置 email 块 + caddy validate（ACME_EMAIL 非空） | result: pass | note: "Valid configuration"；空值场景仍保持不预置(避免 Caddy 报错)
- TC-5 | stack: shell | command: install.sh 连跑两次（不同 OURO_DOMAIN） | result: pass | note: 第二次检测 ENV_PREEXISTED，DOMAIN 保留 localhost(未被 changed.example 覆盖) + 守卫提示

## 7. Change Requests (append-only)
- （无）
- 2026-06-29 S0012 关闭（active→completed，Closure Reason: `delivered`）：p1–p4 交付——`deploy/update.sh`(热备/--tag/健康等待/回滚)、安装器集成、文档接入、multi-agent-review 6 个 P2 修复;并实地发布 **v0.3.0**(镜像 `ghcr.io/cauu/ouro-pass:0.3.0`,`latest` 同 digest 跟随,多架构 amd64+arm64,钉版本一行安装可用)。剩余 P3 加固(/backups gitignore、digest 钉死镜像/Actions、release latest 限非预发、健康等待 none、env_file 扩散、端口提示、pipefail 等)未做,如需另立新 spec(Previous Spec-ID: S0012)。
- 2026-06-29 评审后修复（p4-1）：multi-agent-review（claude+codex+cursor，`code_review/S0012-deploy-stack/`）提出 6 个值得修的 P2,经用户批准后修复（见 p4-1）。未修项：若干 P3（`/backups/` gitignore、release `latest` 限非预发、Actions/镜像 digest 钉死、健康等待 `none`、env_file 扩散 PG 密码、端口占用提示、init 提示 docker stake-hash、pipefail）留作后续可选加固。cursor 两个 P1（eval 注入、trap 退出码）实测为误报,不修。
