# Wallet network the Admin login expects (mainnet|preprod|preview). When unset,
# the wallet network guard is skipped and the backend owner-key check is the gate.
VITE_ISSUER_NETWORK=preprod

# Optional: issuer origin for the Vite dev proxy (default http://localhost:8080).
# VITE_ISSUER_ORIGIN=http://localhost:8080
