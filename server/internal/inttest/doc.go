// Package inttest contains Postgres-backed integration tests gated behind the
// "integration" build tag (see pg_concurrency_test.go). It has no non-test code;
// this file only keeps the package non-empty under default build tags.
package inttest
